apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-cert
spec:
  secretName: todo-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: todo.local
  dnsNames:
    - todo.local